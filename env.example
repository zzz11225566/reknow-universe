# ============================================================
# 炼知 ReKnow · 后端密钥（复制为 env 后填写；env 已被 .gitignore 忽略，不会入库）
# 只放服务端：浏览器端永远不出现任何密钥（项目红线）
# ============================================================

# ---- 知乎开放平台（developer.zhihu.com）----
# 在知乎开放平台个人中心获取 Access Secret，形如 40 位十六进制串
ZHIHU_ACCESS_TOKEN=
# 可选：接入域名（默认 https://developer.zhihu.com）
# ZHIHU_API_BASE=https://developer.zhihu.com
# 可选：知乎直答模型（默认 zhida-thinking-1p5，可选 zhida-fast-1p5 / zhida-agent）
# ZHIHU_ZHIDA_MODEL=zhida-thinking-1p5

# ---- DeepSeek ----
# 形如 sk-<32 位十六进制>，共 35 字符。形态不符时后端会自动改用知乎直答
DEEPSEEK_API_KEY=
# 可选：模型（默认 deepseek-chat）
#   deepseek-chat   快（~1.5s）、话术紧凑，推荐
#   deepseek-v4-pro 推理型：先写思考过程再出正文（~5-6s，更贵）；
#                   后端会自动抬高 token 预算，但演示不建议
# DEEPSEEK_MODEL=deepseek-chat
# 可选：只给「AI 抬杠」单独指定模型（不填则同 DEEPSEEK_MODEL）
# DEEPSEEK_DEBATE_MODEL=deepseek-chat
# 可选：接入地址（默认 https://api.deepseek.com）
# DEEPSEEK_BASE_URL=https://api.deepseek.com

# ---- 服务 ----
# PORT=8787
