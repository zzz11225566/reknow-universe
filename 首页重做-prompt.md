# Prompt：按设计稿重做「炼知 ReKnow v6」首页视图与顶栏

你是资深前端工程师。请修改我项目根目录下的 **`index（修改）.html`**（文件名含全角括号，注意不是 `index.html`），把**首页视图（收藏与选题）和顶栏**按我提供的设计稿重做视觉。这是一个知乎黑客松项目「炼知 ReKnow v6 · 炼金宇宙版」——把「收藏」炼成「回答」的学习应用。

> 注意：本项目有两套文件——原版（`index.html` / `app.js` / `boot.js` / `universe.js`）和修改版（`index（修改）.html` / `app（修改）.js` / `boot（修改）.js` / `universe（修改）.js`）。**本次只操作修改版**，且它加载的是 `app（修改）.js` 和 `boot（修改）.js`（见文件底部 `<script>` 标签），分析渲染逻辑时请读这两个文件。

## 绝对硬约束（违反任何一条即为失败）

1. **只许修改 `index（修改）.html` 这一个文件**。严禁改动 `app（修改）.js` / `boot（修改）.js` / `universe（修改）.js` / `data.js` / `server.js`，也严禁改动原版四件套，严禁新增任何文件。
2. **保持单文件、零依赖、纯离线**：双击 `index（修改）.html`（file://）必须完整可用。禁止任何 CDN、外部字体、网络图片、importmap、构建步骤。
3. **不得删除或重命名任何现有元素的 `id`、class 钩子或 JS 事件绑定**。`app（修改）.js` / `boot（修改）.js` / `universe（修改）.js` 依赖它们。
4. 所有现有功能必须照常工作，不允许功能回退（见下方验收清单）。

## 关键架构事实（动手前必须先读代码确认）

- 首页内容（hero 横幅、分类书架 `cat-shelf`、题卡 `tcard`、底部挑选栏 `pick-bar`、自定义选题面板 `custom-panel` 等）是由 `app（修改）.js` 在运行时**动态渲染**进 `<section id="view-home">` 的。重做前请先读 `app（修改）.js`，搜 `view-home`、`hero-band`，确认哪些文案/结构是 JS 生成的。
  - **JS 生成的文案和结构一律不改**（你也改不了），只能通过 `<style>` 里的 CSS 重绘它们的视觉（现有类名如 `.hero-band` `.cat-shelf` `.shelf-head` `.grid3` `.tcard` `.addcard` `.pick-bar` `.custom-panel` 必须保留并继续被渲染结果命中）。
  - 设计稿中**新增的、JS 没有的静态区块**（如「五步炼知法」卡片条），作为静态 HTML 插入 `<main>` 内、放在 `#view-home` 段落旁边（建议放在它后面），并用纯 CSS 控制只在首页视图激活时显示，例如借助 `main:has(#view-home.on)` 选择器；不得干扰 `#view-home` 的正常渲染。
- 顶栏按钮的 id 与作用（**id 不许动，只改样式和文字**）：
  - `#vBtnHome` 📚 收藏与选题、`#vBtnMind` 🧠 思维图谱、`#vBtnLib` 🧰 知识库、`#vBtnUni`（当前文字为「🏆 炼金成就」，即**炼金宇宙入口**，由 boot（修改）.js 接线打开 3D 宇宙覆盖层 `#uniShell`，必须保留且醒目）。
  - `#lvTxt`（等级文字「炼金学徒」）、`#xpBar`（经验条）、`#xpTxt`（数值）、`#streakTxt`（连炼计数）、`#btnSnd`（音效）、`#btnHelp`（新手引导）、`#btnReset`（清空进度）、`.acc`（账号区「小许 · 在线」）。
- `<main>` 里还有 `#view-flow`（五步学习流）、`#view-mind`、`#view-lib` 三个视图，以及页脚的 `.appfoot`、看山挂件 `.fox-help`、弹层 `#ovOnboard` / `#ovAward` 等，均不在本次重做范围，但不得破坏。

## 设计要求（严格按设计稿）

### 顶栏（`.appbar`）
- 白色底、底部细分隔线，整体更轻更通透；高度约 60px，内容最大宽度 1180px 居中。
- 左侧品牌：蓝色方形圆角「炼」字 logo +「炼知 ReKnow」加粗，右侧小字标语「把收藏炼成回答」。
- 四个视图切换按钮做成**胶囊分段控件**样式（圆角药丸组，激活项白底浅灰药丸）：
  - 「收藏与选题」「思维图谱」「知识库」为常规项，文字前带线性小图标（📚/🧠/🧰 可用线性 SVG 或保留 emoji，但要精致）。
  - 「炼金宇宙」为**常驻金色高亮项**：浅金渐变底（约 `#fdf3d7`→`#f7e3ac`）+ 金色描边 + 深金文字（约 `#a06a00`），让它一眼可辨。激活状态另有白底药丸，两者叠加时仍要清晰。
- 右侧状态区，依次为：
  - 炉火值胶囊：`🔥 炼金学徒 + 细经验条 + 120`（经验条是细长的金色/橙色渐变进度条，数值由 `#xpTxt` 驱动）。
  - 「？」帮助圆钮、「…」更多圆钮（更多钮可只做空样式态）、账号区：蓝色圆形「许」头像 +「小许 · 在线」（保留绿色在线点）。
  - 「连炼 N 篇」(`#streakTxt`) 与音效钮 `#btnSnd`、清空钮 `#btnReset` 保留，可合并进「…」菜单的观感但**不得删除节点与 id**（可以视觉弱化）。

### 首页 Hero 区（重绘 `.hero-band`）
- 白色大圆角卡片（约 20px 圆角、极浅投影、浅灰边框），内部留白充足。
- 大标题（JS 生成的 `.big` 文字**不改内容**，只改样式）：衬线感字体（离线可用栈：`"Songti SC","STSong","SimSun",serif`，Windows 下回退宋体），字号约 `clamp(1.6rem, 3vw, 2.2rem)`，字重 800，深黑 `#1a1a1a`；标题中的强调词用品牌蓝（约 `#0f88eb`）+ 下划线式高亮。
- 副标题 `.sub`：灰色 `#4b4b4b`，行高 1.8，最大宽度约 60em。
- 操作区 `.mini-actions`（JS 渲染，只改样式）改为横排三个控件，圆角药丸：
  - 搜索框：白色底、浅灰描边、左侧放大镜图标，占位文字「搜索选题 / 关键词 / 答主…」；
  - **主按钮「✦ 帮我挑一篇」**：纯黑（`#1a1a1a`）实心药丸、白色文字、白色星标，悬浮微提亮；
  - 次按钮「自定义选题」：白底浅灰描边药丸。
  - 这三个控件的具体元素若由 JS 生成，就用 CSS 按上述风格重绘现有节点；样式覆盖不到的结构差异以功能可用为先。
- 设计稿 hero 内无看山形象图：现有 `.hero-band img.herofox` 若由 JS 生成则保留节点但可弱化/隐藏（`opacity` 降低或窄屏才显示），不得删除。

### 「五步炼知法」静态卡片条（新增静态区块）
- 位置：首页 hero 之下、分类书架之上（静态插入，仅首页激活时显示）。
- 区块标题：「五步炼知法」+ 灰色小字副标题「每篇收藏都走这条路，学完就算真正掌握」。
- 五张等宽横排卡片（响应式：窄屏自动换行/横向滚动），白底、浅灰边框、大圆角、极浅投影：
  1. 选料——从收藏夹挑一篇值得学的好回答（图标 ⛏️）
  2. 拆解——拆成知识点卡片，逐条读懂（图标 🧩）
  3. 学懂——闯关 / 快答 / 听讲 / 与 AI 开杠（图标 🎓）
  4. 图谱——把理解结成自己的思维图谱（图标 🗺）
  5. 回流——写成回答发回知乎，完成闭环（图标 ➶）
- 每张卡片：左上角蓝色线性图标，右上角竖排灰色汉字编号「壹 / 贰 / 叁 / 肆 / 伍」，中部加粗卡名，下方灰色一行说明。
- 纯展示，不绑定点击逻辑（不要写 JS）。

### 全局基调
- 白色内容卡片 + 浅灰页面底（约 `#f5f6f8`），大量留白，圆角统一（12–20px），边框 `#e6e8eb`，投影极浅。
- 主色：黑 `#1a1a1a`（主按钮/大标题）、品牌蓝 `#0f88eb`（链接/强调/图标）、金 `#e6a23c`（炼金宇宙入口/经验条/编号点缀）。
- 其余视图（学习流/思维图谱/知识库）和弹层样式**不要做整体翻新**，但因全局 CSS 变量（`:root`）改动影响到的部分需自查一遍，不能破版。

## 验收清单（全部通过才算完成）

1. 直接**双击** `index（修改）.html`（file://，无任何服务器）打开，控制台无报错。
2. 顶栏四个 tab 切换正常；「炼金宇宙」金色入口点击后 3D 宇宙（`#uniShell`）正常打开、可返回。
3. 经验条 `#xpBar` / 数值 `#xpTxt` / 连炼 `#streakTxt` 随学习动作更新；帮助、音效、清空按钮均可用。
4. 首页搜索、「帮我挑一篇」「自定义选题」、题卡点击、分类书架、底部挑选栏全部功能可用。
5. 「五步炼知法」卡片条只在首页显示，切到其他视图时消失，回首页恢复。
6. 宽度 760px 以下移动端不破版（hero、药丸组、卡片条均可换行）。
7. 除 `index（修改）.html` 外零文件改动；无网络请求（DevTools Network 面板除本地文件外为空）。

完成后请汇报：改了哪些 CSS 区块、新增/插入了哪些 HTML、哪些设计稿细节因「JS 生成、不许改 JS」而未能 1:1 还原及原因。


---

## 已知限制（2026-09-12 实际执行后记录 · 复用本 prompt 前必读）

### 文件与架构
- 目标文件是 `index（修改）.html`（文件名含**全角括号**，不是 `index.html`），它加载 `app（修改）.js` / `boot（修改）.js` / `universe（修改）.js`；原版四件套同样不许动。
- 修改版只有 home / flow 两个视图 + 炼金宇宙覆盖层；思维图谱、知识库已并入炼金宇宙（见 `app（修改）.js` 中 showView 的注释）。因此顶栏只有「收藏与选题」「炼金宇宙」两个 tab——设计稿中的「思维图谱 / 知识库」tab 无法 1:1 还原（不许改 JS，加按钮会是死按钮）。
- 首页 DOM（hero、分类书架、题卡、底部挑选栏、自定义选题面板、看山提示条）全部由 `app（修改）.js` 的 `renderHome()` 渲染进 `#view-home`：只能 CSS 重绘，文案与结构不可改。

### 视觉基线（当前生效版本 · 经一次试错后确定）
- 品牌蓝维持原值 `#0f88eb`（`--blue` / `--blue-soft` 未覆盖）；链接、图标、强调色用原品牌蓝
- 4px 方角保留（`--r:4px` + 各硬编码圆角逐一覆盖；`.btn` / `.tag` / `.searchbox` 也已方角化）——此项已获确认，不要再改回圆角
- 阴影保留：hero / 五步卡 / 题卡 / 挑选栏均为白卡 + 极浅投影（`--shadow` 原值未动）
- 标题用衬线宋体（`--serif:"Songti SC","STSong","SimSun",serif`，hero 大标题 / logo「炼」字 / 壹贰叁肆伍编号）；金色保留给「炼金宇宙」入口与经验条
- **已回滚的试错（2026-09-12，勿再尝试）**：知乎蓝 `#056de8` 全局覆盖、去阴影细线分割、黑体标题——三方案均已落地并整体回滚，原因是观感不如原方案。复用本 prompt 时以本节基线为准
- 「五步炼知法」是 `<main>` 里的静态区块，经 `#view-home{display:contents}` + flex `order` + `main:has(#view-home.on)` 插入 hero 与书架之间、仅首页显示。**改动首页布局时必须保住这套机制**（`#view-home.on>*:not(.hero-band){order:2}` 等规则）。

### 文案与功能（JS 生成 / JS 接线，不可改）
- hero 主按钮文案为「🎲 帮我挑一篇 →」（`#hbRandom`），非设计稿的「✦ 帮我挑一篇」
- 等级文案（见习学徒/炼金学徒）、炉火数值、连炼数分别由 `#lvTxt` / `#xpTxt` / `#streakTxt` 驱动
- 设计稿的「…」更多按钮无对应功能，实际保留 🔊 / ？ / ↺ 三个真实功能钮（节点与 id 不可删，可视觉弱化）
- hero 右下看山 GIF（`img.herofox`）按设计稿以 CSS 隐藏，JS 节点仍在
- 「自定义选题」入口不在 hero，在各分类书架末尾的「加一个自己的选题」卡片（addcard → `openCustom()`）

### 已做的兼容修复与验收技巧
- ≤520px 极窄屏顶栏允许折行（`.bar` / `.statwrap` `flex-wrap` + `main{padding-top:108px}`），修复了存量横向溢出（原版同样存在且更严重）
- 截图验收前先置 `localStorage.rkIntro=1` 跳过新手引导弹层，否则遮罩会挡住页面
- headless Chrome 最小窗口宽度约 497px，验证 390px 移动布局需用 iframe 模拟（外层 420px 窗口 + 390px iframe + `--allow-file-access-from-files`）
